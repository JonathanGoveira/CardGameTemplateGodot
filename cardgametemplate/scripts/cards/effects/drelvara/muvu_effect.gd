extends DomEffect

class_name MuvuEffect

func executar (card, board_state) -> void:
	# O Dom ainda não executa a lógica,
	# Mas isso aqui garante que o jogo não quebre.
	print("Muvu entrou em campo.")
